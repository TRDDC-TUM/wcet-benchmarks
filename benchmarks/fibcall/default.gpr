project Default is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("fibcall.adb");

   package Compiler is
      for Switches ("Ada") use ("-g", "-O2");
      for Switches ("ada") use ("-gnatwa", "-g", "-gnata", "-gnatf");
   end Compiler;

end Default;

