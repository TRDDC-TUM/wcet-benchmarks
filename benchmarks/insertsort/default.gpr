project Default is

   package Compiler is
      for Switches ("Ada") use ("-g", "-O2");
      for Switches ("ada") use ("-gnatwa", "-g", "-gnatf", "-gnata");
   end Compiler;

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("insertsort.adb");

end Default;

