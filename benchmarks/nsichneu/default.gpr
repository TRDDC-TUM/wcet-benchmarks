project Default is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("nsichneu.adb");

   package Compiler is
      for Switches ("Ada") use ("-g", "-O2");
      for Switches ("ada") use ("-gnatwa", "-g", "-gnatf", "-gnato");
   end Compiler;

end Default;

