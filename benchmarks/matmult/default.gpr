project Default is

   package Compiler is
      for Switches ("Ada") use ("-g", "-O2");
   end Compiler;

   for Source_Dirs use ("src");
   for Main use ("matmult.adb");
   for Object_Dir use "obj";

end Default;

